services:
  protonmail-bridge:
    image: ghcr.io/spi3/protonmail-bridge-docker:latest
    ports:
      - 1025:25/tcp
      - 1143:143/tcp
    restart: unless-stopped
    volumes:
      - protonmail:/root
volumes:
  protonmail:
    name: protonmail
